/**
 * Re-exports from src/utils/firebaseConfig.ts
 * This provides backward compatibility for code still importing from this location.
 */

// Re-export all Firebase configuration from the utils directory
export * from '../utils/firebaseConfig';
export { default } from '../utils/firebaseConfig'; 